[relays.relay-network1]
hostname="relay-network1"
port="9080"
tls=true
tlsca_cert_path="/opt/relay/credentials/docker/ca-cert.pem"

[relays.relay-network2]
hostname="relay-network2"
port="9083"
tls=true
tlsca_cert_path="/opt/relay/credentials/docker/ca-cert.pem"

[relays.relay-corda]
hostname="relay-corda"
port="9081"
tls=true
tlsca_cert_path="/opt/relay/credentials/docker/ca-cert.pem"

[relays.relay-corda2]
hostname="relay-corda2"
port="9082"
tls=true
tlsca_cert_path="/opt/relay/credentials/docker/ca-cert.pem"
