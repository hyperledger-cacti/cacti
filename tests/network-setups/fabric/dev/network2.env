# Environment variable setup for the Fabric network configured as a source network.
# These variables can be overridden by defining through the command line the same
# variable name with a new value, this environment file will first check whether
# the variable is define and if not it will assign a default value.

COMPOSE_PROJECT_NAME=network2
FABRIC_CFG_PATH=${APP_ROOT}/fabric/shared/network2
NW_CFG_PATH=${APP_ROOT}/fabric/shared/network2

CA_ORG1_PORT=${N2_CA_ORG1_PORT}
CA_ORG2_PORT=${N2_CA_ORG2_PORT}
CA_ORDERER_PORT=${N2_CA_ORDERER_PORT}

FABRIC_CA_VOL=${NW_CFG_PATH}/fabric-ca/org1

CHAINCODELISTENADDRESS=${N2_CHAINCODELISTEN_PORT}
ORDERER_PORT=${N2_ORDERER_PORT}
COUCHDB0_PORT=${N2_COUCHDB0_PORT}
COUCHDB1_PORT=${N2_COUCHDB1_PORT}
PEER_ORG1_PORT=${N2_PEER_ORG1_PORT}
PEER_ORG2_PORT=${N2_PEER_ORG2_PORT}
