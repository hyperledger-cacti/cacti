# Environment variable setup for the Fabric network configured as a source network.
# These variables can be overridden by defining through the command line the same
# variable name with a new value, this environment file will first check whether
# the variable is define and if not it will assign a default value.

COMPOSE_PROJECT_NAME=network1
FABRIC_CFG_PATH=${APP_ROOT}/fabric/shared/network1
NW_CFG_PATH=${APP_ROOT}/fabric/shared/network1

CA_ORG1_PORT=${N1_CA_ORG1_PORT}
CA_ORG2_PORT=${N1_CA_ORG2_PORT}
CA_ORDERER_PORT=${N1_CA_ORDERER_PORT}

FABRIC_CA_VOL=${NW_CFG_PATH}/fabric-ca/org1

#CORE_PEER_ADDRESS=7051
#ORDERER_LISTENPORT=7050
#PEER_LISTENADDRESS=7051

CHAINCODELISTENADDRESS=${N1_CHAINCODELISTEN_PORT}
ORDERER_PORT=${N1_ORDERER_PORT}
COUCHDB0_PORT=${N1_COUCHDB0_PORT}
COUCHDB1_PORT=${N1_COUCHDB1_PORT}
PEER_ORG1_PORT=${N1_PEER_ORG1_PORT}
PEER_ORG2_PORT=${N1_PEER_ORG2_PORT}