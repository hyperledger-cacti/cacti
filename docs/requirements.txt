mkdocs
mkdocs-material
mike
mkdocs-swagger-ui-tag
# Optional: bundles the pandoc binary (pip-installable) for exporting the
# whitepaper to PDF or other formats without a system-wide pandoc install.
pypandoc-binary